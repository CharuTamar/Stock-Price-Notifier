<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Get Stock market updates</title>
    <style>
        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 10%;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
            font-weight: 400;
        }
        .user-input {
            padding: 15px 40px;
            border: 2px solid rgb(10, 160, 10);
            border-radius: 5px;
            margin-bottom: 10px;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }
        .sub-btn {
            padding: 14px 20px;
            background-color: rgb(10, 160, 10);
            border: 2px solid rgb(10, 160, 10);
            border-radius: 7px;
            color: white;
            cursor: pointer;
            font-size: 1rem;
        }
        .fund{
            padding: 15px 20px;
            border: 2px solid rgb(10, 160, 10);
            border-radius: 3px;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }

    </style>
</head>
<body>

    <div class="container">
        <div class="input-form">
            <h3>When any stock crahes from NIFTY 50, You'll receive an update</h3>
            <form action="/" method="post">
                <input class="user-input" type="text" name="name" placeholder="Enter your name" autocomplete="off" required>
                <input class="user-input" type="email" name="email" placeholder="Enter your email" required>
                <select name="funds" class="fund">
                        <option value="hundred">NIFTY 100</option>
                        <option value="five">NIFTY 500</option>
                </select>
                <input class="sub-btn" type="submit" value="Submit">
            </form>
        </div>
    </div>

    
</body>
</html>